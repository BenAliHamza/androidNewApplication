<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/doctor_patients_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_background"
    android:paddingHorizontal="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    tools:context="tn.esprit.presentation.home.DoctorPatientsFragment">

    <!-- Header: title + subtitle -->
    <TextView
        android:id="@+id/text_doctor_patients_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/doctor_patients_title"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/color_on_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/text_doctor_patients_subtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/doctor_patients_subtitle"
        android:textSize="13sp"
        android:textColor="@color/color_on_background_secondary"
        app:layout_constraintTop_toBottomOf="@id/text_doctor_patients_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Progress bar (centered, shown while loading) -->
    <ProgressBar
        android:id="@+id/doctor_patients_progress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/text_doctor_patients_empty"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/doctor_patients_empty"
        android:textSize="14sp"
        android:textColor="@color/color_on_background_secondary"
        android:gravity="center"
        android:visibility="gone"
        android:paddingTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/text_doctor_patients_subtitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible" />

    <!-- Patients list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_doctor_patients"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingTop="16dp"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/text_doctor_patients_subtitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_doctor_patient" />

</androidx.constraintlayout.widget.ConstraintLayout>
